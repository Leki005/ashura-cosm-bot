GRILL-ME REVIEW — Grok 4.3
============================================================

[GRILL-1] CRITICAL | handlers/admin.py:120 | CALLBACK DATA COLLISION: admin_accept_quick_ пересекается с admin_accept_ по префиксу, aiogram роутер может вызвать неправильный хендлер | Добавить точный F.data.startswith("admin_accept_quick_") перед общим admin_accept_ и сделать callback_data уникальными (admin_accept_quick_{id} и admin_accept_full_{id})

[GRILL-2] CRITICAL | handlers/admin.py:450 | BROADCAST SENDS RAW HTML без экранирования пользовательского текста | Перед отправкой broadcast_text использовать html_escape на всём контенте от пользователя, а не только на некоторых полях

[GRILL-3] HIGH | handlers/client.py:890 | RACE CONDITION на активную запись: get_active_booking + create без транзакции и SELECT FOR UPDATE | Обернуть проверку и создание в async with session.begin(): + добавить pessimistic lock или unique constraint на (user_id, status) в pending/confirmed

[GRILL-4] CRITICAL | handlers/privacy.py:95 | MIDDLEWARE BYPASSES privacy check если session=None — просто пускает дальше | Убрать возврат await handler при session is None; кидать RuntimeError или явно требовать session в middleware

[GRILL-5] CRITICAL | handlers/ai_consultant.py:210 | AI_HISTORY растёт бесконечно — _photo_session_count и история в state не ограничены | Добавить жёсткий лимит 20 сообщений в state и чистить историю при превышении; добавить TTL на _photo_session_count

[GRILL-6] CRITICAL | database.py:780 | SQL INJECTION в apply_migrations через f-string с миграциями | Полностью убрать f-строки в SQL, использовать Alembic или параметризованные запросы; migrations должны быть статическими .sql файлами

[GRILL-7] CRITICAL | config.py:310 | SSL VERIFICATION отключена по умолчанию (get_ssl_verify возвращает False) | По умолчанию ставить verify=True; отключать только через явный флаг PROXY_INSECURE=true и логировать предупреждение

[GRILL-8] HIGH | bot.py:85 | MemoryStorage теряет FSM state при рестарте — все BookingState, AdminAcceptState и т.д. пропадут | Перейти на RedisStorage или SQLAlchemyStorage с персистенцией

[GRILL-9] CRITICAL | handlers/client.py:1020 | TOCTOU race на создании записи — проверка даты/времени и insert без unique constraint | Добавить unique constraint на (date, time, service_id) + try/except IntegrityError с откатом

[GRILL-10] HIGH | database.py:55 | Даты хранятся как String(100) вместо DateTime — сломается сортировка и сравнение | Заменить все строковые даты на DateTime, мигрировать данные

[GRILL-11] HIGH | bot.py:40 | Нет log rotation — logs/bot.log будет расти до заполнения диска | Добавить RotatingFileHandler с maxBytes=10*1024*1024 и backupCount=5

[GRILL-12] HIGH | handlers/client.py:1350 | catch-all handler перехватывает чужие FSM состояния (BookingState.* попадает в общий text handler) | Добавить state=State() фильтры на все хендлеры или использовать ~F.state.is_(None) для catch-all

[GRILL-13] CRITICAL | handlers/privacy.py:180 | privacy_revoke не отменяет активные записи — consent отозван, а Booking висят | При revoke добавить массовую отмену всех ACTIVE_BOOKING_STATUSES с уведомлением

[GRILL-14] MEDIUM | utils/grok.py:95 | lru_cache на load_system_prompt не инвалидируется при изменении файла | Убрать @lru_cache или добавить mtime check + cache_clear при hot-reload

[GRILL-15] HIGH | utils/grok.py:45 | _daily_requests не персистится — после рестарта счётчик обнуляется и лимит можно обойти | Хранить daily_requests в БД (таблица ai_usage) или Redis

[GRILL-16] CRITICAL | handlers/client.py:760 | Отсутствует валидация callback_data на booking_time — можно подставить любую строку и сломать БД | Строгая валидация через regex и whitelist перед вызовом format_time_from_callback

[GRILL-17] HIGH | database.py:310 | Отсутствует индекс на bookings(user_id, status) — запросы get_active_booking будут тормозить при >10k записей | Добавить Index('idx_booking_user_status', Booking.user_id, Booking.status)

[GRILL-18] CRITICAL | handlers/admin.py:620 | admin_accept_choice_keyboard генерирует callback без проверки прав — любой пользователь может угадать id и принять | Добавить проверку is_admin внутри хендлера + подпись callback (HMAC)

[GRILL-19] HIGH | utils/helpers.py:480 | check_throttle хранит все user_id в памяти без ограничения по времени жизни >1 часа | Добавить TTL на каждую запись (например, dict с timestamp и очистка старше 1 часа)

[GRILL-20] CRITICAL | bot.py:155 | ErrorNotifyHandler может вызвать бесконечную рекурсию при ошибке отправки в ERROR_NOTIFY_ID | Обернуть отправку в try/except и не логировать ошибки из этого handler'а

[GRILL-21] HIGH | config.py:200 | PROXY_URL и PROXY_AUTO одновременно — логика выбора прокси не детерминирована и может привести к утечке | Убрать PROXY_AUTO, оставить только явный PROXY_URL или механизм выбора

[GRILL-22] MEDIUM | handlers/skin_anamnesis.py:300 | _safe_edit_markup глотает все TelegramBadRequest без логирования — можно пропустить критические ошибки | Логировать исключение хотя бы на WARNING уровне

[GRILL-23] CRITICAL | handlers/common.py:85 | show_client_home не проверяет, что message не из callback — при вызове из CallbackQuery упадёт | Добавить проверку типа и использовать answer или edit_message соответственно

[GRILL-24] HIGH | utils/grok.py:140 | Глобальная сессия _grok_session не закрывается при shutdown — утечка соединений | Добавить cleanup в on_shutdown и закрывать сессию явно

[GRILL-25] CRITICAL | database.py:410 | apply_migrations выполняется без транзакции и может оставить БД в inconsistent состоянии | Обернуть все миграции в begin() + rollback при ошибке

[GRILL-26] HIGH | handlers/client.py:1400 | Отсутствует throttling на callback "menu_booking" — можно спамить и создавать тысячи FSM | Добавить check_throttle и на CallbackQuery (по data)